# RegenNexus UAP - Core Dependencies
# Install with: pip install -r requirements.txt

# Core requirements (required)
pyyaml>=6.0
websockets>=10.0
aiohttp>=3.8.0
pycryptodome>=3.15.0

# Recommended (for full functionality)
click>=8.0.0
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn>=0.20.0

# Optional - install separately as needed:
# pip install pyserial        # Arduino support
# pip install aiomqtt         # MQTT support
# pip install RPi.GPIO        # Raspberry Pi (on Pi only)
# pip install azure-iot-device # Azure IoT support
